These codes generate logs in response to situation.
If you wanna generate it,you have to insert these code below in each step you confirm.

--------------------------------------
logging.debug('debug')
logging.info('info')
logging.warning('warning')
logging.error('error')
logging.critical('critical')
--------------------------------------
